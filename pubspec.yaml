name: ink2latex
description: Handwriting to LaTeX notes app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.5
  flutter: '>=3.41.0'

dependencies:
  flutter:
    sdk: flutter
  google_mlkit_digital_ink_recognition: ^0.14.0
  flutter_math_fork: ^0.7.2
  path_provider: ^2.1.0
  http: ^1.2.0
  provider: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
